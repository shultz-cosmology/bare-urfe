{
  "mcpServers": {
    "shultz-bare-urfe": {
      "command": "python",
      "args": [
        "bare_urfe.py"
      ],
      "env": {
        "MODEL_VERSION": "5.1",
        "GEOMETRIC_FLOOR": "0.7500",
        "MATTER_ATTRACTOR": "0.7814",
        "STABILITY_PROFILE": "TOROIDAL_ATTRACTOR_CORRIDOR"
      },
      "description": "Executes the Shultz Bare-URFE v5.1 engine. Verifies the 0.7500 geometric floor and identifies the 0.7814 matter attractor. Models 4D-to-3D recursive projection via the Spectral Trace Lock."
    }
  }
}
